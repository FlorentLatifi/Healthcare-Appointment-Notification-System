# ────────────────────────────────────────────────────────────────────
# LOCAL DOCKER ONLY — copy to .env (gitignored) for docker-compose.yml
# Production uses .env.secrets + config/production.env on the server.
# See docs/secrets-management.md
# ────────────────────────────────────────────────────────────────────

DB_PASSWORD=YourStr0ng!Pass
JWT_SECRET=super-secret-key-that-is-at-least-32-characters-long

# Stripe test keys only for local/dev
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXX

BOOTSTRAP_ADMIN_PASSWORD=

# Production host layout (do not commit filled values):
#   /opt/healthcare/.env.secrets          # secrets only, mode 600
#   /opt/healthcare/config/production.env # non-secrets from git
